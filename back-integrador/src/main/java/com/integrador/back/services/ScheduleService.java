package com.integrador.back.services;

import com.integrador.back.model.entities.Schedule;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.Optional;

public interface ScheduleService {
//    public Page<Schedule> getSchedules(Date dateStart, Optional<String> name, Pageable pageable);
//    Page<Schedule> findAll (Pageable pageable);
//    Schedule save(ScheduleCreateRequest schedule);
//    Optional<Schedule>update(ScheduleCreateRequest schedule, Long id);
}
